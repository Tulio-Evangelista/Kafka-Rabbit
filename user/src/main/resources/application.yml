spring:
  application:
    name: user
  datasource:
    url: jdbc:postgresql://localhost:5435/ms-user-ms
    username: postgres-user
    password: postgres-password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
    addresses: amqps://raccoon.lmq.cloudamqp.com
    username: wxealxph
    password: 1zeO_CDADNen0wpjkbXvbMM1mU-wn6Iv
    virtual-host: wxealxph

server:
  port: 8081