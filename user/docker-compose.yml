version: '3.8'
services:
  postgres:
    image: postgres:16
    container_name: ms_user_db
    environment:
      POSTGRES_DB: ms_user_ms
      POSTGRES_USER: postgres-user
      POSTGRES_PASSWORD: postgres-password
    ports:
      - "5435:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: